Rockin' Right-Click is a simple Chrome extension that adds an option to the right-click context menu allowing the user to select an entire paragraph. 
It is mostly useless, given the existence of the triple-click feature built into many applications.
